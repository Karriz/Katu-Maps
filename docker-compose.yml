services:
  martin:
    image: ghcr.io/maplibre/martin:1.12.0
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data/processed:/data:ro
    command:
      - /data/tampere.mbtiles
      - /data/terrain.mbtiles
    restart: unless-stopped
